# ==============================================================================
# 🚨 PILOT STRATEGY CONFIGURATION (实盘试点配置)
# ------------------------------------------------------------------------------
# ⚠️ WARNING: 此配置直接控制实盘资金。
# 所有的风控参数必须是显式的数值 (Explicit Numbers)，严禁使用模糊的标签。
# ==============================================================================

gateway:
  # 使用环境变量注入，如果未设置则回退到默认值 (本地开发或 Docker 内部)
  # 生产环境 IP: 172.19.141.255
  zmq_url: "${MT5_ZMQ_URL:-tcp://172.19.141.255:5555}"
  pub_url: "${MT5_PUB_URL:-tcp://172.19.141.255:5556}"
  timeout: 5000
  retry_attempts: 3

strategies:
  - name: "Production_Strategy_V1"
    active: true
    symbols: ["EURUSD", "GBPUSD"]
    timeframe: "H1"

    # 1. 显式模型绑定 (Model Binding)
    model:
      type: "xgboost"
      # 指向具体的模型文件，不要让系统去猜
      path: "models/baseline_v1.json"
      min_confidence: 0.6

    # 2. 显式硬性风控 (Hard Risk Limits)
    # 架构师要求：必须看到最大的亏损是多少
    risk:
      profile: "conservative"     # 仅作为日志标签
      max_lot_size: 0.01          # [硬限制] 单笔最大手数
      stop_loss_pips: 20          # [硬限制] 强制止损点数
      max_daily_loss_usd: 50.0    # [硬限制] 当日最大亏损额 (美元)
      max_leverage: 30            # [硬限制] 计算仓位时的最大杠杆限制

    logging:
      level: "INFO"
      log_file: "/app/logs/trading.log"

metadata:
  version: "1.1.0"
  last_updated: "2026-01-02"
  approved_by: "AI_Architect_Gemini"
