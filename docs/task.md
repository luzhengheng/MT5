/task  
(Role: Project Manager / System Architect)  
  
**TASK #[ID]: [任务名称]**  
**Protocol**: v4.2 (Agentic-Loop)  
**Priority**: [High/Critical]  
  
## 1. 任务定义 (Definition)  
* **核心目标**: [一句话描述要做什么]  
* **实质验收标准 (Substance)**:  
    * [ ] 功能: [例如: 实时行情延迟 < 5ms]  
    * [ ] 审计: Gate 2 日志显示 Token 消耗 > 0。  
    * [ ] 韧性: 无静默失败，异常即使捕获也需打印 FATAL 日志。  
* **归档路径**: `docs/archive/tasks/TASK_[ID]/`  
  
## 2. 交付物矩阵 (Deliverable Matrix)  
*Agent 必须确保以下文件全部通过 Gate 1 静态检查*  
  
| 类型 | 文件路径 | **Gate 1 刚性验收标准** |  
| :--- | :--- | :--- |  
| **代码** | `src/...` | 无 Pylint 错误; 逻辑符合 PEP8; 类型提示完整。 |  
| **测试** | `scripts/...` | 运行通过; 覆盖率 > 80%; **必须包含断言(Assert)**。 |  
| **日志** | `.../VERIFY_LOG.log` | 包含 `[SUCCESS]` 标记; 包含性能指标数据。 |  
| **文档** | `.../COMPLETION_REPORT.md` | 记录了 "Fix Iterations" (即修正了多少轮才通过)。 |  
  
## 3. 执行计划 (Agentic Execution Plan)  
  
### Step 1: 基础设施铺设 (Setup)  
* [ ] 创建归档目录。  
* [ ] **TDD 优先**: 编写 `audit_current_task.py`。  
    * *指令*: 必须包含针对本次任务特定的检查逻辑（如：检查某个 JSON 字段是否存在）。  
  
### Step 2: 核心开发 (Development)  
* [ ] 编写业务代码。  
* [ ] 运行自测: `python3 [script] | tee .../VERIFY_LOG.log`。  
  
### Step 3: 智能闭环审查 (The Loop) ⚠️ 核心步骤  
* **执行指令**: `python3 gemini_review_bridge.py`  
* **Agent 自我修正协议**:  
    > **当遇到 Gate 1 报错时**: 不要等待用户指令。读取错误 -> 定位行号 -> 修正代码 -> 立即重跑。  
    > **当遇到 Gate 2 拒绝时**: 仔细阅读 AI 架构师的 Markdown 建议。按建议修改代码 -> 立即重跑。  
    > **退出条件**: 看到 `BOTH GATES PASSED` 且 Token 消耗日志可见。  
  
### Step 4: 全域同步 (Sync)  
* **Git**: `git add . && git commit -m "feat([ID]): [summary]" && git push origin main`  
* **Notion**: `/run python3 scripts/update_notion.py [ID] Done`  
  
## 4. 异常处理 (Escalation)  
* 如果连续 **3次** 修正后仍无法通过 Gate 1 或 Gate 2，请停止操作并输出：  
  `🔴 HUMAN HELP NEEDED: Stuck on [Error Name]. I have tried [Action 1], [Action 2].`  
