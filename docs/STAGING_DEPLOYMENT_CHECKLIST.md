# 预生产环境部署检查清单

**日期**: 2026-01-19
**目标**: 在预生产环境验证所有部署流程和监控机制
**状态**: 🟢 **待执行**

---

## 📋 部署前检查

### 代码准备

- [ ] Git分支确认
  ```bash
  # 验证所有修改已提交
  git status  # 应显示 clean
  git log --oneline -5  # 验证最近5个commit
  ```

- [ ] 代码编译检查
  ```bash
  # Python编译检查
  python3 -m py_compile src/gateway/*.py
  python3 -m py_compile scripts/ops/*.py
  python3 -m py_compile scripts/ai_governance/*.py
  ```

- [ ] 依赖项检查
  ```bash
  # 验证所有导入可用
  python3 -c "from src.gateway.json_gateway import JsonGatewayRouter"
  python3 -c "from src.gateway.zmq_service import ZmqGatewayService"
  python3 -c "from scripts.ops.notion_bridge import validate_token"
  python3 -c "from scripts.ai_governance.unified_review_gate import ArchitectAdvisor"
  ```

- [ ] 配置文件检查
  ```bash
  # 验证所有配置文件存在且有效
  ls -la config/
  python3 -c "import config; config.validate()"
  ```

**检查完成**: ❌ / ✅

---

## 🔧 环境准备

### 预生产环境配置

- [ ] 服务器资源分配
  - CPU: 4核 (同生产)
  - 内存: 8GB (同生产)
  - 磁盘: 100GB (足够日志空间)
  - 网络: 与生产环境相同配置

- [ ] 数据库准备
  ```bash
  # 创建预生产数据库
  ./scripts/setup_staging_database.sh

  # 导入测试数据
  ./scripts/import_test_data.sh

  # 验证数据完整性
  ./scripts/verify_database.sh
  ```

- [ ] 依赖服务配置
  ```bash
  # ZMQ配置
  - [ ] ZMQ服务器运行正常
  - [ ] 端口开放 (5555/5556)
  - [ ] 防火墙规则配置

  # MT5配置
  - [ ] MT5模拟账户准备
  - [ ] 交易权限配置
  - [ ] 网络连接测试

  # Notion配置
  - [ ] API Token有效
  - [ ] Workspace权限正确
  - [ ] 网络连接测试
  ```

- [ ] 监控系统配置
  ```bash
  # 部署监控代理
  ./scripts/deploy_monitoring_agent.sh

  # 配置告警规则
  ./scripts/configure_alerting_rules.sh

  # 验证数据收集
  ./scripts/verify_metrics_collection.sh
  ```

**检查完成**: ❌ / ✅

---

## 📊 监控准备

### 指标采集配置

- [ ] 关键指标监控
  ```
  [ ] 订单重复率 (应=0%)
  [ ] 推送失败率 (应<0.5%)
  [ ] P99延迟 (应<5s)
  [ ] 重试成功率 (应>95%)
  [ ] 错误率 (应<0.1%)
  ```

- [ ] 性能指标监控
  ```
  [ ] CPU使用率
  [ ] 内存使用率
  [ ] 磁盘使用率
  [ ] 网络带宽
  [ ] 数据库连接数
  ```

- [ ] 业务指标监控
  ```
  [ ] 订单执行速度
  [ ] 推送成功率
  [ ] 重试成功率
  [ ] API响应时间
  [ ] 数据同步延迟
  ```

### 告警规则配置

- [ ] 关键告警
  ```
  - 订单重复率 > 0.1%
  - 推送失败率 > 2%
  - P99延迟 > 10s
  - 错误率 > 1%
  - CPU使用率 > 80%
  ```

- [ ] 警告告警
  ```
  - 推送失败率 > 1%
  - P99延迟 > 5s
  - 内存使用率 > 70%
  - 重试成功率 < 85%
  ```

- [ ] 通知配置
  ```
  [ ] 邮件通知配置
  [ ] 短信通知配置 (关键告警)
  [ ] Slack集成
  [ ] 值班人员列表
  ```

**检查完成**: ❌ / ✅

---

## 🚀 部署执行

### 部署步骤

```bash
# Step 1: 备份现有版本
[ ] ./scripts/backup_current_version.sh

# Step 2: 部署新版本
[ ] docker build -t mt5-crs:resilience-staging .
[ ] docker push mt5-crs:resilience-staging

# Step 3: 创建部署计划
[ ] kubectl create deployment mt5-crs-resilience-staging \
    --image=mt5-crs:resilience-staging

# Step 4: 验证部署
[ ] kubectl get pods
[ ] kubectl describe pod <pod-name>

# Step 5: 运行健康检查
[ ] ./scripts/health_check.sh
```

**部署完成**: ❌ / ✅

---

## ✅ 部署验证

### 功能验证

- [ ] Notion同步模块
  ```bash
  # 测试Token验证
  python3 -c "
  from scripts.ops.notion_bridge import validate_token
  result = validate_token()
  assert isinstance(result, bool)
  print('✅ Token验证成功')
  "

  # 测试任务推送
  [ ] 发送测试任务到Notion
  [ ] 验证任务成功创建
  ```

- [ ] LLM API模块
  ```bash
  # 测试API调用
  python3 -c "
  from scripts.ai_governance.unified_review_gate import ArchitectAdvisor
  advisor = ArchitectAdvisor()
  # 测试API连接
  print('✅ LLM API连接正常')
  "
  ```

- [ ] MT5网关模块
  ```bash
  # 测试订单执行
  [ ] 发送测试订单
  [ ] 验证订单执行成功
  [ ] 检查订单重复率 (应=0%)

  # 测试ZMQ通信
  [ ] 测试Socket接收
  [ ] 测试Socket发送
  [ ] 验证延迟 (应<500ms)
  ```

### P1修复验证

- [ ] Double Spending防护
  ```bash
  # 压力测试
  python3 tests/gateway/stress_test_order_duplication.py \
    --orders 100 --timeout-rate 0.2

  # 验证结果
  [ ] 重复率 = 0%
  [ ] 100订单无重复
  ```

- [ ] ZMQ超时对齐
  ```bash
  # 延迟测试
  python3 tests/gateway/stress_test_zmq_latency.py \
    --requests 1000 --concurrent 50

  # 验证结果
  [ ] P99延迟 < 5s
  [ ] 成功率 > 99%
  ```

### 监控验证

- [ ] 指标收集正常
  ```bash
  # 验证指标已收集
  [ ] 订单重复率指标存在
  [ ] 延迟分布指标存在
  [ ] 成功率指标存在
  ```

- [ ] 告警规则生效
  ```bash
  # 测试告警触发
  [ ] 触发关键告警
  [ ] 验证通知收到
  [ ] 确认告警消息准确
  ```

**验证完成**: ❌ / ✅

---

## 📈 性能基线建立

### 基线测试

```bash
# 运行完整的性能测试
[ ] ./scripts/run_baseline_tests.sh

# 记录基线指标
[ ] P50延迟: ___ ms
[ ] P99延迟: ___ ms
[ ] P999延迟: ___ ms
[ ] 吞吐量: ___ ops/sec
[ ] 错误率: ___ %
[ ] 重试成功率: ___ %
```

### 基线数据保存

```bash
# 保存基线数据
[ ] ./scripts/save_baseline_metrics.sh

# 生成基线报告
[ ] ./scripts/generate_baseline_report.sh
```

**基线完成**: ❌ / ✅

---

## 📊 48小时稳定性测试

### 长期运行测试

```bash
# 运行48小时监控
[ ] 启动长期监控脚本
[ ] 每2小时记录一次指标
[ ] 检查是否有内存泄漏
[ ] 检查是否有性能衰减
```

### 稳定性标准

```
每6小时指标检查:
[ ] 订单重复率保持=0%
[ ] 推送失败率稳定 (<0.5%)
[ ] 延迟分布稳定
[ ] 没有异常尖峰
[ ] 内存使用稳定
[ ] CPU使用稳定
```

**稳定性测试完成**: ❌ / ✅

---

## 🔄 回滚流程验证

### 回滚测试

```bash
# 测试快速回滚
[ ] 记录当前版本状态
[ ] 执行回滚脚本
[ ] 验证旧版本恢复
[ ] 检查数据完整性
[ ] 验证旧版本功能正常
```

### 回滚时间验证

```
回滚应在15分钟内完成:
[ ] 停止新版本流量: ___ 分钟
[ ] 恢复数据库: ___ 分钟
[ ] 启动旧版本: ___ 分钟
[ ] 验证系统: ___ 分钟
总耗时: ___ 分钟 (应<15)
```

**回滚验证完成**: ❌ / ✅

---

## 📝 文档验证

- [ ] 部署指南正确
  ```bash
  [ ] 步骤能否在生产环境执行
  [ ] 命令是否有效
  [ ] 输出是否符合预期
  ```

- [ ] 监控指南正确
  ```bash
  [ ] 指标定义是否清晰
  [ ] 告警规则是否完整
  [ ] 处理流程是否明确
  ```

- [ ] 回滚指南正确
  ```bash
  [ ] 回滚步骤是否准确
  [ ] 恢复流程是否完整
  [ ] 验收标准是否清晰
  ```

- [ ] 应急预案完整
  ```bash
  [ ] 覆盖所有关键场景
  [ ] 处理流程明确
  [ ] 联系方式准确
  ```

**文档验证完成**: ❌ / ✅

---

## 👥 团队准备

- [ ] 部署负责人确认
  ```
  姓名: ___________
  电话: ___________
  邮箱: ___________
  ```

- [ ] 开发团队通知
  ```
  [ ] 发送部署计划
  [ ] 确认理解修改内容
  [ ] 准备技术支持
  ```

- [ ] 运维团队通知
  ```
  [ ] 发送部署指南
  [ ] 确认环境准备
  [ ] 准备监控告警
  ```

- [ ] 值班安排确认
  ```
  部署周期间:
  [ ] 第1周 5% - 值班人员确认
  [ ] 第2周 25% - 值班人员确认
  [ ] 第3周 50% - 值班人员确认
  [ ] 第4周 100% - 值班人员确认
  ```

**团队准备完成**: ❌ / ✅

---

## 🎯 最终审查

### 预生产验收

在执行下面所有检查项都✅之后,才能开始生产部署:

```
□ 代码准备:     ✅ 完成
□ 环境准备:     ✅ 完成
□ 监控准备:     ✅ 完成
□ 部署执行:     ✅ 完成
□ 功能验证:     ✅ 完成
□ P1修复验证:   ✅ 完成
□ 监控验证:     ✅ 完成
□ 性能基线:     ✅ 完成
□ 稳定性测试:   ✅ 完成
□ 回滚验证:     ✅ 完成
□ 文档验证:     ✅ 完成
□ 团队准备:     ✅ 完成
```

### 审批签字

**预生产环境部署审批**:

```
部署负责人签字: ____________    日期: __________
开发经理签字:   ____________    日期: __________
运维经理签字:   ____________    日期: __________
产品经理签字:   ____________    日期: __________
```

**审批状态**: ⏳ 待签字

---

## 📅 执行计划

| 步骤 | 日期 | 状态 |
|------|------|------|
| 部署前检查 | 2026-01-20 | ⏳ |
| 环境准备 | 2026-01-20 | ⏳ |
| 监控配置 | 2026-01-20 | ⏳ |
| 部署执行 | 2026-01-20 | ⏳ |
| 部署验证 | 2026-01-20 | ⏳ |
| 稳定性测试 | 2026-01-21-22 | ⏳ |
| 最终审查 | 2026-01-22 | ⏳ |
| 准备就绪 | 2026-01-23 | ⏳ |

---

**检查清单制定日期**: 2026-01-19
**目标完成日期**: 2026-01-23
**预生产部署负责人**: [待指定]

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>
