# Protocol v4.4: 闭环自动化开发流程 (Closed-Loop Automation Protocol)

**版本**: 4.4
**发布日期**: 2026-01-18
**状态**: Beta - Task #125 实施中
**继承自**: Protocol v4.3 (Zero-Trust Edition)

---

## 📋 概述 (Overview)

Protocol v4.4 定义了一套完整的 **人机协作自动化开发闭环**，旨在将代码执行、审查、文档同步、计划生成和任务注册等环节串联成一个无缝工作流。

该协议的核心理念是：**最大化自动化，最小化人工干预，但保留人类的最终决策权**。

---

## 🔄 闭环的5个阶段 (5 Stages of the Loop)

```
┌─────────────────────────────────────────────────────────────┐
│                    THE CLOSED LOOP                          │
│                 (Protocol v4.4 Architecture)                │
└─────────────────────────────────────────────────────────────┘

    ╔════════════════════════════════════════════════════════╗
    ║  Stage 1: EXECUTE (执行阶段)                           ║
    ║  ├─ Agent 执行工作 (编码/测试)                         ║
    ║  ├─ 生成工件 (artifacts)                              ║
    ║  └─ 记录 Session 日志                                 ║
    ╚════════════════════════════════════════════════════════╝
                            │
                            ▼
    ╔════════════════════════════════════════════════════════╗
    ║  Stage 2: REVIEW (审查阶段)                            ║
    ║  ├─ Gate 1: 静态检查 (Pylint/PEP8)                    ║
    ║  ├─ Gate 2: AI 审查 (unified_review_gate.py)         ║
    ║  ├─ 新增: 智能补丁生成 (doc_patch mode)               ║
    ║  └─ 输出: 审查意见 + 修正指令 (JSON)                  ║
    ╚════════════════════════════════════════════════════════╝
                            │
                            ▼
    ╔════════════════════════════════════════════════════════╗
    ║  Stage 3: SYNC (同步阶段)                              ║
    ║  ├─ 自动应用文档补丁                                  ║
    ║  ├─ 更新中央文档 (Central Command Docs)              ║
    ║  ├─ 更新任务档案                                     ║
    ║  └─ 生成 COMPLETION_REPORT.md                         ║
    ╚════════════════════════════════════════════════════════╝
                            │
                            ▼
    ╔════════════════════════════════════════════════════════╗
    ║  Stage 4: PLAN (计划阶段)                              ║
    ║  ├─ Gate 2.0 plan_mode 生成后续工单                   ║
    ║  ├─ 输出符合 task.md 模板的 Markdown                  ║
    ║  ├─ 自动解析优先级与依赖关系                          ║
    ║  └─ 准备推送到 Notion                                 ║
    ╚════════════════════════════════════════════════════════╝
                            │
                            ▼
    ╔════════════════════════════════════════════════════════╗
    ║  Stage 5: REGISTER (注册阶段)                          ║
    ║  ├─ notion_bridge.py 推送新工单                       ║
    ║  ├─ 自动设置优先级/状态/依赖                          ║
    ║  ├─ 人类暂停点: 等待确认                              ║
    ║  └─ 下一循环可以开始                                  ║
    ╚════════════════════════════════════════════════════════╝
```

---

## 📍 详细流程说明 (Detailed Process)

### Stage 1: EXECUTE (执行阶段)

**目标**: Agent 完成工作，记录所有执行痕迹。

**执行步骤**:
1. Agent 读取当前任务定义 (from `scripts/read_task_context.py`)
2. 编写代码/测试/文档
3. 使用 `tee VERIFY_LOG.log` 记录所有输出
4. 运行单元测试，确保覆盖率 > 80%
5. 记录 Session UUID (从 Claude Code 的日志获取)

**输出物**:
- 修改的源代码
- 单元测试及覆盖率报告
- `VERIFY_LOG.log` (包含时间戳 + Token 消耗)

**检查点**:
```bash
grep -E "Token Usage|UUID|PASS|FAIL" VERIFY_LOG.log
```

---

### Stage 2: REVIEW (审查阶段)

**目标**: 自动审查代码和文档，生成修正指令。

**执行步骤**:

```bash
# Gate 1: 静态检查
pylint src/execution/*.py --disable=C0111,W0612 | tee -a VERIFY_LOG.log

# Gate 2: AI 审查 + 补丁生成
python3 scripts/ai_governance/unified_review_gate.py \
  --task-id 125 \
  --mode review+doc_patch \
  --input-files "src/execution/..." \
  --output-format json \
  | tee -a VERIFY_LOG.log
```

**新增特性: doc_patch Mode**

Unified Gate 2.0 现在能输出结构化的补丁指令：

```json
{
  "task_id": "125",
  "review_status": "PASS_WITH_IMPROVEMENTS",
  "artifacts": {
    "code_patches": [
      {
        "file": "src/execution/concurrent_trading_engine.py",
        "action": "replace",
        "search_pattern": "old_code_snippet",
        "replacement": "new_code_snippet",
        "reason": "Simplify asyncio lock usage"
      }
    ],
    "doc_updates": [
      {
        "file": "docs/archive/tasks/[MT5-CRS] Central Comman.md",
        "action": "append_section",
        "section": "9.5 自动化闭环工作流",
        "content": "..."
      }
    ]
  },
  "tokens_consumed": {
    "input": 8523,
    "output": 12445,
    "total": 20968
  }
}
```

**输出物**:
- `VERIFY_LOG.log` (审查记录 + Token 消耗)
- `review_patches.json` (修正指令)

**检查点**:
```bash
# 验证审查通过
grep "review_status.*PASS" review_patches.json

# 验证 Token 消耗被记录
grep "tokens_consumed" review_patches.json
```

---

### Stage 3: SYNC (同步阶段)

**目标**: 将审查意见和补丁自动应用到文档和代码。

**执行步骤**:

```bash
# Step 1: 应用代码补丁 (如果有)
python3 scripts/apply_patches.py \
  --input review_patches.json \
  --target-type code \
  --auto-apply

# Step 2: 应用文档补丁
python3 scripts/apply_patches.py \
  --input review_patches.json \
  --target-type documentation \
  --auto-apply

# Step 3: 生成完成报告
python3 scripts/generate_completion_report.py \
  --task-id 125 \
  --session-log VERIFY_LOG.log \
  --output docs/archive/tasks/TASK_125/COMPLETION_REPORT.md
```

**COMPLETION_REPORT.md 模板**:

```markdown
# Task #125 完成报告

**任务ID**: TASK#125
**状态**: ✅ COMPLETE
**完成时间**: 2026-01-18 14:32:15 UTC
**Session UUID**: [from logs]

## 执行摘要
- 总代码行数: XXX
- 测试覆盖率: XX%
- Gate 1 结果: PASS
- Gate 2 结果: PASS
- Token 消耗: XX,XXX

## 交付物清单
- [ ] 代码补丁已应用
- [ ] 文档已同步更新
- [ ] 测试已通过
- [ ] 安全审查已通过

## 下一步任务预生成
- 已生成 Task #126 初稿
- 依赖关系: Task #125 -> Task #126
```

**输出物**:
- 更新的源代码和文档
- `docs/archive/tasks/TASK_125/COMPLETION_REPORT.md`

**检查点**:
```bash
grep -E "Session UUID|PASS|Token Consumed" \
  docs/archive/tasks/TASK_125/COMPLETION_REPORT.md
```

---

### Stage 4: PLAN (计划阶段)

**目标**: 智能生成下一个任务工单。

**执行步骤**:

```bash
python3 scripts/ai_governance/unified_review_gate.py \
  --task-id 125 \
  --mode plan \
  --completed-task-path docs/archive/tasks/TASK_125/COMPLETION_REPORT.md \
  --output-format markdown \
  --output-file docs/archive/tasks/TASK_126/TASK_126_PLAN.md
```

**自动生成的工单应包含**:
- 任务ID (自动递增, 125 → 126)
- 角色定义 (Role: System Architect)
- 核心目标 (一句话)
- 5个实质验收标准
- 4-5个执行步骤 (符合 task.md 模板)
- 架构师备注 (Architect's Notes)
- 关键指标

**工单模板验证**:
```bash
# 检查工单是否符合 task.md 标准
python3 scripts/validate_task_template.py \
  docs/archive/tasks/TASK_126/TASK_126_PLAN.md
```

**输出物**:
- `docs/archive/tasks/TASK_126/TASK_126_PLAN.md` (新工单)

**检查点**:
```bash
# 验证工单有效性
grep "^TASK #" docs/archive/tasks/TASK_126/TASK_126_PLAN.md
grep "^## [1-5]" docs/archive/tasks/TASK_126/TASK_126_PLAN.md
```

---

### Stage 5: REGISTER (注册阶段)

**目标**: 自动将新工单推送到 Notion，并等待人类确认。

**执行步骤**:

```bash
# Step 1: 从 Markdown 解析工单信息
python3 scripts/ops/notion_bridge.py \
  --action parse \
  --input docs/archive/tasks/TASK_126/TASK_126_PLAN.md \
  --output task_metadata.json

# Step 2: 验证 Notion Token
python3 scripts/ops/notion_bridge.py \
  --action validate-token \
  --token $NOTION_TOKEN

# Step 3: 创建 Notion Page
python3 scripts/ops/notion_bridge.py \
  --action push \
  --input task_metadata.json \
  --database-id $NOTION_TASK_DATABASE_ID \
  --output notion_page_id.json

# Step 4: 暂停 - 等待人类确认
echo "⏸️  HALTED: Task #126 已推送到 Notion"
echo "   请在 Notion 中审查并点击 'Approve' 后再继续"
echo "   页面链接: $(cat notion_page_id.json | jq -r '.page_url')"
```

**人类确认流程**:
1. 打开 Notion 中自动生成的 Task #126 页面
2. 审查任务定义、验收标准、执行步骤
3. 点击 "Approve" 按钮 (由 Script 监听)
4. 系统自动继续到下一循环

**输出物**:
- Notion 中的新 Task Page
- `notion_page_id.json` (包含 page_url)

**检查点**:
```bash
# 验证 Notion 推送成功
grep "page_id.*created" notion_page_id.json
grep "status.*PENDING_APPROVAL" notion_page_id.json
```

---

## 🔐 安全与限制条件 (Security & Constraints)

### 1. Notion API 限制

- **Rate Limit**: 每秒最多 3 个请求
- **实现**: `time.sleep(0.35)` between API calls
- **重试机制**: 最多 3 次重试，指数退避 (1s, 2s, 4s)

```python
# 示例实现
import time

def notion_api_call(fn, *args, **kwargs):
    for attempt in range(3):
        try:
            return fn(*args, **kwargs)
        except RateLimitError:
            wait_time = 2 ** attempt
            time.sleep(wait_time)
    raise Exception("Notion API Rate Limit exceeded after 3 attempts")
```

### 2. Token 安全

**禁止事项**:
- ❌ 提交 `NOTION_TOKEN` 到 Git
- ❌ 打印 `NOTION_TOKEN` 到日志
- ❌ 在脚本中硬编码 Token

**合规检查**:
```bash
# 在推送前检查
grep -r "NOTION_TOKEN\|ntn_" src/ scripts/ --include="*.py" && \
  echo "⚠️  WARNING: Token found in source code" || \
  echo "✅ SAFE: No tokens in source code"
```

### 3. 回滚机制

如果 `dev_loop.sh` 中途失败，应该有自动恢复：

```bash
# 自动回滚到上一个状态
git stash
git reset --hard HEAD

# 记录失败原因
echo "Failure at stage: $FAILED_STAGE" >> dev_loop.log
echo "Error details: $ERROR_MESSAGE" >> dev_loop.log
```

---

## 📊 State Machine (状态机)

```
[START]
  │
  ├─→ [EXECUTE] → (error) ──→ [DEBUG] ──┐
  │                              │       │
  │                              └──────┤
  │                                     │
  ├─→ [REVIEW] → (fail) ──→ [PATCH] ──→ [REVIEW] (retry)
  │      │
  │      └─→ (pass)
  │           │
  ├─→ [SYNC] (apply patches, update docs)
  │      │
  ├─→ [PLAN] (generate next task)
  │      │
  ├─→ [REGISTER] (push to Notion)
  │      │
  ├─→ [HALT] (wait for human)
  │      │
  ├─→ [HUMAN_APPROVAL] (人类确认)
  │      │
  └─→ [NEXT_CYCLE] (回到 EXECUTE)
```

---

## 🎯 关键指标 (KPIs)

| 指标 | 目标 | 说明 |
|------|------|------|
| **Loop Duration** | < 5 min | 从 EXECUTE 到 HALT 的总时间 |
| **Gate 1 Pass Rate** | 100% | 静态检查无错误 |
| **Gate 2 Pass Rate** | 95%+ | AI 审查通过率 |
| **Doc Patch Accuracy** | 100% | 自动补丁正确应用率 |
| **Notion Sync Latency** | < 3s | API 响应时间 |
| **Human Approval Time** | < 2h | 人类审核时间 |

---

## 🚀 初始化 Checklist

在启动 Protocol v4.4 闭环前，确保：

- [ ] `.env` 中包含有效的 `NOTION_TOKEN`
- [ ] `.env` 中包含有效的 `NOTION_DATABASE_ID` (用于 Task)
- [ ] `scripts/ops/notion_bridge.py` 已开发并通过单元测试
- [ ] `scripts/ai_governance/unified_review_gate.py` 支持 `--mode=doc_patch` 和 `--mode=plan`
- [ ] `scripts/dev_loop.sh` 已编写完成
- [ ] Git 仓库干净 (no uncommitted changes)
- [ ] 所有依赖已安装 (`pip install notion-client`)

---

## 📝 文档版本历史

| 版本 | 日期 | 变更 |
|------|------|------|
| v4.4 | 2026-01-18 | 初始版本，定义 5 阶段闭环 + 人机协作 |
| v4.3 | 2026-01-17 | Zero-Trust Edition (前置版本) |

---

**Protocol Owner**: System Architect (Claude Code)
**Last Updated**: 2026-01-18
**Status**: Beta - 正式启动 Task #125
