**工单标题：** 项目全局域名访问规范实施与切换（域名解析已完成）——v1  
**目标：**域名解析已全部配置完成，现要求项目全局工作彻底切换到域名访问方式。从即日起，所有操作、配置、文档、脚本、聊天记录中**非必要情况下禁止直接使用 IP 地址访问服务器**，必须统一使用已绑定域名，确保安全性、一致性和专业性。  
**目标：**域名解析已全部配置完成，现要求项目全局工作彻底切换到域名访问方式。从即日起，所有操作、配置、文档、脚本、聊天记录中**非必要情况下禁止直接使用 IP 地址访问服务器**，必须统一使用已绑定域名，确保安全性、一致性和专业性。  
**域名与服务器对应关系（固定，不可更改）**：  
**域名与服务器对应关系（固定，不可更改）**：  
1. **中枢服务器**（主控服务器，Claude Code CLI 等）  
2. **中枢服务器**（主控服务器，Claude Code CLI 等）  
    * 域名：www.crestive-code.com  
3. **推理服务器**（模型推理、实时预测服务等）  
4. **推理服务器**（模型推理、实时预测服务等）  
    * 域名：www.crestive.com  
5. **训练服务器**（模型训练、回测、大数据处理等）  
6. **训练服务器**（模型训练、回测、大数据处理等）  
    * 域名：www.guangzhoupeak.com  
**要求：**  
* 所有团队成员、AI 代理、自动化脚本必须遵守“域名优先”原则  
* 更新项目所有相关文档和配置，使用域名替代任何残留的 IP  
* 在文档中明确记录该规范，便于后续遵守  
**具体执行步骤（请严格按顺序执行）：**  
1. **全局搜索并替换残留 IP**  
2. **全局搜索并替换残留 IP**  
    * 在项目根目录执行全局搜索，查找三台服务器的旧 IP 地址  
    * 将所有出现的 IP 替换为对应域名：  
        * 中枢服务器旧 IP → www.crestive-code.com  
        * 推理服务器旧 IP → www.crestive.com  
        * 训练服务器旧 IP → www.guangzhoupeak.com  
    * 重点检查文件：  
        * CONTEXT.md（包括所有分支）  
        * .cursor/rules.md  
        * README.md、部署文档、配置文件  
        * MQL5 源码、Python 脚本、API 调用地址  
        * SSH 配置文件（如 ~/.ssh/config）、Cursor 设置备注  
3. **配置本地 SSH 便捷域名登录（推荐，提升效率）**  
4. **配置本地 SSH 便捷域名登录（推荐，提升效率）**  
    * 在本地电脑的 ~/.ssh/config 文件中添加以下内容（每台电脑执行一次）：Host crestive-code  
    *     HostName www.crestive-code.com  
    *     User root  
    *     Port 22  # 如改过端口请填写实际端口  
    *   
    * Host crestive-inference  
    *     HostName www.crestive.com  
    *     User root  
    *     Port 22  
    *   
    * Host guangzhoupeak  
    *     HostName www.guangzhoupeak.com  
    *     User root  
    *     Port 22  
    *   
    * 保存后，以后登录只需：ssh crestive-code      # 中枢服务器  
    * ssh crestive-inference # 推理服务器  
    * ssh guangzhoupeak      # 训练服务器  
    *   
5. **在项目文档中添加全局访问规范**  
6. **在项目文档中添加全局访问规范**  
    * 在项目根目录的 README.md 或 CONTEXT.md 顶部添加以下规范说明：### 服务器访问全局规范（强制执行）  
    * - 所有服务器访问必须通过域名进行，禁止直接使用 IP 地址（除非域名解析故障等极端情况）。  
    * - 中枢服务器：www.crestive-code.com  
    * - 推理服务器：www.crestive.com  
    * - 训练服务器：www.guangzhoupeak.com  
    * - SSH 登录推荐使用 ~/.ssh/config 配置的别名（crestive-code / crestive-inference / guangzhoupeak）  
    *   
    * 在 .cursor/rules.md 中添加 AI 行为约束：- 当提到服务器访问时，始终使用域名，不得输出裸 IP。  
    * - 如需提供登录命令，使用域名或 SSH 别名。  
    *   
7. **验证切换完成**  
8. **验证切换完成**  
    * 使用域名完整测试三台服务器的 SSH 访问  
    * 检查项目关键文件（CONTEXT.md 等）是否已无裸 IP 残留  
    * 在 Cursor 新会话中输入 @CONTEXT.md，确认 AI 回复中服务器相关内容已使用域名  
9. **完成后回复确认**请回复以下内容：  
10. **完成后回复确认**请回复以下内容：  
```
项目全局域名访问规范实施工单（v1）已执行完成：
- 所有项目文件、配置、文档已彻底清除裸 IP，统一替换为对应域名
- 本地 SSH 已配置域名别名，便捷登录
- 项目文档已添加“域名优先、禁止直接 IP 访问”强制规范
- AI 行为规则已更新，强制使用域名表述
- 已使用域名完整验证三台服务器访问正常
从即日起，项目全局工作正式进入域名化访问阶段，安全性与规范性大幅提升。

```
**结束语**域名解析已就位，本工单重点在于“执行切换”和“建立规范”。执行完成后，项目将彻底告别 IP 直连时代，所有操作更加专业、安全、可追溯。  
现在请直接执行本工单，确保全局彻底切换。  
