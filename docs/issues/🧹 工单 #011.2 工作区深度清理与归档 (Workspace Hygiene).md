🧹 工单 #011.2: 工作区深度清理与归档 (Workspace Hygiene)  
优先级: P1 (高 - 环境维护)  
类型: Chore / Maintenance  
依赖: 工单 #010.9 (Notion Nexus 已部署), 工单 #011.1 (AI 规则已固化)  
目标: 移除已同步至 Notion/GitHub 的冗余文件，清理一次性脚本，将项目体积瘦身，聚焦核心代码与当前文档。  
🛡️ 清理策略 (Safety First)  
为了防止误删，我们采取 "先归档，后删除" 的策略：  
 * 创建 _archive_20251222/ 目录。  
 * 将所有被判定为冗余的文件移动到该目录。  
 * 确认系统正常运行后，您可以随时手动删除归档目录。  
🗑️ 待清理目标清单  
1. 冗余文档 (已同步 Notion)  
 * docs/issues/*.md: 所有的旧工单完成报告 (#006 - #010.9)。  
 * docs/reports/*.md: 旧的部署报告、清理报告。  
 * docs/SESSION_*.md: 所有的会话过程记录。  
 * docs/*_SUMMARY.md: 各类阶段性总结 (如 P1_QUICK_START.md, PROGRESS_SUMMARY.md)。  
 * *.txt: 根目录下的临时统计文件 (如 PROJECT_STATUS.txt)。  
2. 过期上下文包  
 * exports/*: 所有的 AI_PROMPT_*.md, CONTEXT_SUMMARY_*.md 等历史导出文件。  
 * gemini_docs_package.tar.gz: 旧的打包文件。  
3. 一次性部署/迁移脚本 (任务已完成)  
 * notion_nexus_deploy.py: 部署脚本。  
 * notion_nexus_fixed.py: 修复脚本。  
 * migrate_knowledge.py: 迁移脚本。  
 * populate_nexus_db.py: 填充脚本。  
 * create_issue_*.py: 旧的工单创建脚本。  
 * locate_nexus.py, check_nexus_db.py: 调试脚本。  
 * auto_create_nexus.py, create_new_nexus.py: 早期尝试脚本。  
 * restore_main_page.py, simple_restore.py, clean_*.py: 修复脚本。  
4. 临时日志与缓存  
 * var/log/*: 旧日志。  
 * var/reports/*: 旧的本地运行报告。  
💎 必须保留的核心文件 (Whitelist)  
 * src/: 核心源代码 (不动)。  
 * bin/: 操作脚本 (run_backtest, train_model 等)。  
 * config/: 配置文件。  
 * data/: 数据文件。  
 * 根目录工具:  
   * gemini_review_bridge.py: AI 审查核心。  
   * nexus_with_proxy.py: Notion 代理核心。  
   * update_notion_from_git.py: 同步脚本。  
   * setup_github_notion_sync.py: Hook 配置。  
   * export_context_for_ai.py: 上下文导出工具。  
   * check_sync_status.py: 状态检查。  
 * 核心文档:  
   * README.md  
   * QUICK_START.md  
   * HOW_TO_USE_GEMINI_REVIEW.md  
   * AI_RULES.md (新)  
   * .cursorrules (新)  
   * .gitignore  
🚀 执行指令 (发给 Claude)  
请复制以下指令发送给 Claude，一键执行清理：  
请执行工单 #011.2：工作区深度清理。  
  
请编写并运行一个 shell 脚本来完成以下操作：  
  
1. **创建归档目录**: `_archive_20251222`。  
  
2. **移动冗余文档**:  
   - 将 `docs/issues/`, `docs/reports/` 下的所有文件移入归档。  
   - 将 `docs/` 下以 `SESSION_`, `P0_`, `P1_`, `P2_`, `ITERATION`, `WORK_ORDER_` 开头的文件移入归档。  
   - 保留 `docs/reviews/` (保留最近的 AI 建议)。  
   - 保留 `QUICK_START.md`, `HOW_TO_USE_*.md` 等核心指南。  
  
3. **移动一次性脚本**:  
   - 移动根目录下所有用于 "deploy", "migrate", "create_nexus", "restore", "clean", "check_db" 的一次性 Python 脚本。  
   - **严禁移动**: `gemini_review_bridge.py`, `nexus_with_proxy.py`, `update_notion_from_git.py`, `export_context_for_ai.py`。  
  
4. **清理导出和临时文件**:  
   - 移动 `exports/` 目录下的所有内容。  
   - 移动根目录下的 `*.txt` 文件。  
  
5. **执行清理**:  
   - 运行该脚本。  
  
6. **Git 同步**:  
   - 使用 DevOps 规则提交更改：`chore(maintenance): 清理冗余文件并归档，保留核心代码与文档 #011`。  
   - 并在最后列出根目录剩余的文件列表供我确认。  
  
